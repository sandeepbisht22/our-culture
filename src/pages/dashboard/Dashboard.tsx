// Dashboard.tsx
import React from 'react';
import { Typography } from '@mui/material';

const Dashboard: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Dashboard - Private Route</Typography>
    </div>
  );
};

export default Dashboard;